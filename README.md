Firstly we have to set of instruction given into the docker file Once docker file is ready we will build the image
with help of docker file and we can create docker container using the docker image.

CMD and entrypoint
CMD defines easily overridable default commands or arguments, while ENTRYPOINT defines a fixed executable that always runs when the container starts

Add and Copy

Docker system prune
primarily to reclaim valuable disk space and optimize system performance by removing accumulated, unused Docker objects

Docker inspect
to retrieve low-level, comprehensive configuration and runtime metadata about Docker objects (containers, images, volumes, networks, or plugins) in a structured JSON format

docker buildx
because it is a powerful CLI plugin that extends traditional Docker builds with BuildKit, enabling multi-platform image creation, parallelized execution, and advanced cache management
